# Fox Crypto Trading Bot Monitor - System Summary

## Обзор системы

Система мониторинга торгового бота Fox Crypto - это real-time терминальный интерфейс для отслеживания торговых позиций, событий и статистики криптовалютного торгового бота.

## Состав системы

### 1. Основные компоненты

```
monitor_ui/
├── main.py                      # Точка входа приложения
├── config.py                    # Конфигурация и настройки
├── requirements.txt             # Python зависимости
├── README.md                    # Полная документация
├── QUICKSTART.md               # Руководство быстрого старта
├── install.sh                   # Скрипт установки
├── .env.example                # Пример конфигурации
│
├── database/                    # Слой работы с БД
│   ├── connection.py           # Пул подключений к PostgreSQL
│   ├── queries.py              # SQL запросы
│   └── models.py               # Pydantic модели данных
│
├── services/                    # Бизнес-логика
│   └── data_fetcher.py         # Асинхронная загрузка данных
│
├── ui/                         # Пользовательский интерфейс
│   ├── app.py                  # Главное приложение Textual
│   ├── styles.tcss             # CSS стили
│   └── widgets/                # UI компоненты
│       ├── header.py           # Заголовок со статусом
│       ├── positions_table.py  # Таблица позиций
│       ├── events_stream.py    # Поток событий
│       └── statistics.py       # Панели статистики и алертов
│
└── tests/                      # Тесты (для будущего развития)
```

### 2. Технологический стек

- **Python 3.10+** - основной язык программирования
- **Textual 0.47.1** - фреймворк для терминального UI
- **asyncpg 0.29.0** - асинхронный драйвер PostgreSQL
- **Pydantic 2.5.2** - валидация и сериализация данных
- **Rich 13.7.0** - форматирование текста и цветов
- **PostgreSQL** - база данных с схемой `monitoring`

## Возможности

### ✅ Реализовано

1. **Real-time мониторинг позиций**
   - Отображение всех активных позиций
   - Текущие цены и PnL в реальном времени
   - Визуальные индикаторы (цвета, иконки)
   - Возраст позиций с предупреждениями

2. **Отслеживание Trailing Stop**
   - Статус TS для каждой позиции (✓ active, ⏳ waiting, ○ inactive)
   - Интеграция с таблицей trailing_stop_state
   - Визуальная индикация состояния

3. **Поток событий**
   - Real-time лог всех системных событий
   - Цветовая кодировка по типу события
   - Автоматическая прокрутка к новым событиям
   - Хранение последних 50-100 событий

4. **Статистика и метрики**
   - Статистика за последний час
   - Win rate и общий PnL
   - Количество открытых/закрытых позиций
   - Активные trailing stops

5. **Система алертов**
   - Предупреждения о старых позициях (>12ч, >24ч)
   - Высокая экспозиция
   - Проблемы с подключением к БД

6. **Управление**
   - Горячие клавиши (Q, R, C, P)
   - Пауза/возобновление обновлений
   - Принудительное обновление данных
   - Очистка потока событий

### 🎯 Архитектурные особенности

1. **Асинхронная архитектура**
   - Все операции с БД асинхронные
   - Concurrent polling разных типов данных
   - Не блокирующий UI

2. **Кэширование**
   - Кэш последних полученных данных
   - Устойчивость к временным сбоям БД
   - Быстрые обновления UI

3. **Read-only безопасность**
   - Только чтение из БД
   - Никаких изменений данных
   - Безопасный мониторинг production

4. **Обработка ошибок**
   - Graceful degradation при сбоях БД
   - Отображение статуса подключения
   - Логирование всех ошибок

5. **Производительность**
   - Connection pooling (2-5 подключений)
   - Оптимизированные SQL запросы
   - Минимальное потребление ресурсов

## Требования к базе данных

### Схема monitoring

```sql
-- Основная таблица позиций
monitoring.positions
  - id, symbol, exchange, side
  - quantity, entry_price, current_price
  - stop_loss_price, unrealized_pnl
  - status, opened_at, closed_at
  - has_trailing_stop, has_stop_loss

-- Состояние trailing stop
monitoring.trailing_stop_state
  - id, symbol, exchange, position_id
  - state, is_activated
  - highest_price, lowest_price
  - current_stop_price, activation_price

-- События системы
monitoring.events
  - id, created_at, event_type
  - event_data (JSONB)
  - symbol, exchange, position_id
```

### Необходимые индексы

- `idx_positions_status` на positions(status)
- `idx_positions_symbol` на positions(symbol)
- `idx_positions_opened_at` на positions(opened_at)
- `idx_events_created_at` на events(created_at)

## Установка и запуск

### Быстрый старт

```bash
# 1. Перейти в директорию
cd monitor_ui

# 2. Запустить скрипт установки
./install.sh

# 3. Настроить .env файл
nano .env

# 4. Запустить монитор
source venv/bin/activate
python main.py
```

### Ручная установка

```bash
# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Запустить
python main.py --db-host localhost --db-name fox_crypto
```

## Использование

### Командная строка

```bash
# С настройками по умолчанию
python main.py

# С кастомной БД
python main.py --db-host remote.host --db-port 5432 --db-user trader

# С отладкой
python main.py --log-level DEBUG --log-file monitor.log

# Все опции
python main.py --help
```

### Интерфейс

```
Панель статуса (верх)
├── Статус: ● RUNNING
├── Uptime: 14h 32m
├── Active: 72/150
└── Exposure: $14,400

Главная область (середина)
├── Левая панель (65%)
│   └── Таблица позиций
│       ├── Symbol/Exchange
│       ├── Side (LONG/SHORT)
│       ├── Entry Price
│       ├── Current Price
│       ├── PnL %
│       ├── TS Status
│       └── Age
│
└── Правая панель (35%)
    ├── Statistics (50%)
    │   ├── Opened/Closed
    │   ├── TS Active
    │   ├── Win Rate
    │   └── Total PnL
    │
    └── Alerts (50%)
        └── Warnings & Alerts

Поток событий (низ, 40%)
└── Real-time event log
```

### Горячие клавиши

- `Q` - Выход из приложения
- `R` - Обновить все данные
- `C` - Очистить поток событий
- `P` - Пауза/возобновление
- `↑/↓` - Навигация по таблице

## Производительность

### Целевые метрики

- **Запуск**: < 2 секунды
- **Обновление позиций**: < 100ms
- **Обновление событий**: < 50ms
- **Память**: < 100MB
- **CPU**: < 5% (idle: <1%)

### Интервалы обновления

- Позиции: 1 секунда
- События: 1 секунда
- Статус системы: 5 секунд
- Статистика: 10 секунд

## Конфигурация

### Переменные окружения (.env)

```env
# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fox_crypto
DB_USER=evgeniyyanvarskiy

# Intervals
POSITION_UPDATE_INTERVAL=1.0
EVENT_UPDATE_INTERVAL=1.0
STATS_UPDATE_INTERVAL=10.0

# UI
MAX_EVENTS_DISPLAY=50
POSITION_AGE_WARNING_HOURS=12
POSITION_AGE_CRITICAL_HOURS=24

# Logging
LOG_LEVEL=INFO
LOG_FILE=monitor_ui.log
```

## Troubleshooting

### Проблема: Не подключается к БД

```bash
# Проверить PostgreSQL
pg_isready

# Тест подключения
psql -h localhost -U evgeniyyanvarskiy -d fox_crypto
```

### Проблема: Нет данных

```sql
-- Проверить наличие данных
SELECT COUNT(*) FROM monitoring.positions WHERE status = 'active';
SELECT COUNT(*) FROM monitoring.events WHERE created_at > NOW() - INTERVAL '1 hour';
```

### Проблема: Высокая нагрузка

```env
# Увеличить интервалы в .env
POSITION_UPDATE_INTERVAL=2.0
EVENT_UPDATE_INTERVAL=2.0
STATS_UPDATE_INTERVAL=15.0
```

## Дальнейшее развитие

### Планируется

- [ ] Исторические графики PnL
- [ ] Детальный просмотр позиции (модальное окно)
- [ ] Фильтры и поиск позиций
- [ ] Экспорт данных в CSV
- [ ] Звуковые/визуальные нотификации
- [ ] Multi-page навигация
- [ ] Dashboard для агрегированной статистики
- [ ] Режим компактного отображения

### Возможные улучшения

- Интеграция с Telegram для алертов
- Web-версия интерфейса (Flask/FastAPI)
- Исторический анализ сделок
- Графики производительности
- API для внешней интеграции

## Документация

- **README.md** - Полная документация проекта
- **QUICKSTART.md** - Руководство быстрого старта
- **MONITORING_UI_IMPLEMENTATION_PLAN.md** - Детальный план реализации
- **SYSTEM_SUMMARY.md** - Этот документ

## Поддержка

При возникновении проблем:

1. Проверьте QUICKSTART.md
2. Просмотрите раздел Troubleshooting в README.md
3. Запустите с `--log-level DEBUG`
4. Проверьте логи в monitor_ui.log

## Лицензия и авторство

Часть экосистемы Fox Crypto Trading Bot.

---

**Версия**: 2.0
**Дата создания**: Октябрь 2025
**Статус**: ✅ Production Ready
