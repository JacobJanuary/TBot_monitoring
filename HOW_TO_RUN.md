# Как запустить Monitor UI

## ✅ Система протестирована и готова к запуску!

Все тесты пройдены успешно. Детали в файле `monitor_ui/TEST_RESULTS.md`

## Быстрый запуск

### Вариант 1: Автоматическая установка

```bash
cd monitor_ui
./install.sh
```

Скрипт автоматически:
- Создаст виртуальное окружение
- Установит все зависимости
- Создаст файл `.env` из шаблона

### Вариант 2: Ручная установка

```bash
cd monitor_ui

# Создать виртуальное окружение
python3 -m venv venv

# Активировать окружение
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

## Запуск приложения

### Простой запуск (с настройками по умолчанию)

```bash
cd monitor_ui
source venv/bin/activate
python main.py
```

### Запуск с кастомными параметрами

```bash
# С другим хостом БД
python main.py --db-host remote.server.com

# С логированием в файл
python main.py --log-level DEBUG --log-file monitor.log

# С другой БД
python main.py --db-name my_database --db-port 5433

# Полный пример
python main.py --db-host localhost --db-port 5432 --db-name fox_crypto --db-user evgeniyyanvarskiy --log-level INFO
```

### Все доступные параметры

```
--db-host       Host базы данных (default: localhost)
--db-port       Порт БД (default: 5432)
--db-name       Имя БД (default: fox_crypto)
--db-user       Пользователь БД (default: evgeniyyanvarskiy)
--db-password   Пароль БД (опционально)

--log-level     Уровень логов: DEBUG, INFO, WARNING, ERROR (default: INFO)
--log-file      Файл логов (default: monitor_ui.log)

--pool-min      Минимальный размер пула соединений (default: 2)
--pool-max      Максимальный размер пула (default: 5)
```

## Интерфейс

После запуска вы увидите терминальный интерфейс:

```
┌─────────────────────────────────────────────────────────────────┐
│           FOX CRYPTO TRADING BOT MONITOR v2.0                   │
│  ● RUNNING │ Uptime: 0h 1m │ Active: 68/150 │ Exposure: $... │
├─────────────────────────────────┬───────────────────────────────┤
│    ACTIVE POSITIONS (68)        │   STATISTICS (Last 1h)        │
│ Symbol      Side  Entry Current │   Opened:      21             │
│ BTCUSDT    LONG   ...   ...  ✓  │   Closed:      20             │
│ ...                             │   Win Rate:    60.0%          │
├─────────────────────────────────┴───────────────────────────────┤
│                    EVENT STREAM                                  │
│ 14:32:15 [POSITION_CREATED] BTCUSDT LONG @67890.0              │
│ 14:32:16 [TS_ACTIVATED] ETHUSDT trailing stop activated        │
└─────────────────────────────────────────────────────────────────┘
```

## Управление

### Горячие клавиши

- `Q` - Выход из приложения
- `R` - Принудительное обновление всех данных
- `C` - Очистить поток событий
- `P` - Пауза/возобновление автообновления
- `↑/↓` - Навигация по таблице позиций
- `Ctrl+C` - Экстренный выход

### Панели интерфейса

1. **Заголовок** (верх)
   - Статус системы (RUNNING/STOPPED/ERROR)
   - Время работы (uptime)
   - Количество активных позиций
   - Общая экспозиция в USD

2. **Таблица позиций** (слева, 65%)
   - Все активные торговые позиции
   - Цены входа и текущие
   - PnL в процентах (с цветовой индикацией)
   - Статус Trailing Stop (✓/⏳/○)
   - Возраст позиции

3. **Статистика** (справа вверху)
   - Открыто позиций за последний час
   - Закрыто позиций за последний час
   - Активных trailing stops
   - Win rate (процент прибыльных)
   - Общий PnL

4. **Алерты** (справа внизу)
   - Предупреждения о старых позициях (>12ч)
   - Критические алерты (>24ч)
   - Высокая экспозиция

5. **Поток событий** (низ, 40%)
   - Real-time лог всех событий
   - Цветовая кодировка по типу
   - Автопрокрутка к новым событиям

## Конфигурация через .env

Для постоянных настроек создайте файл `.env`:

```bash
cd monitor_ui
cp .env.example .env
nano .env
```

Пример содержимого `.env`:

```env
# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fox_crypto
DB_USER=evgeniyyanvarskiy
DB_PASSWORD=

# Update intervals (seconds)
POSITION_UPDATE_INTERVAL=1.0
EVENT_UPDATE_INTERVAL=1.0
STATUS_UPDATE_INTERVAL=5.0
STATS_UPDATE_INTERVAL=10.0

# UI settings
MAX_EVENTS_DISPLAY=50
POSITION_AGE_WARNING_HOURS=12
POSITION_AGE_CRITICAL_HOURS=24

# Logging
LOG_LEVEL=INFO
LOG_FILE=monitor_ui.log
```

## Проверка работоспособности

### Перед первым запуском

1. Проверьте что PostgreSQL запущен:
   ```bash
   pg_isready
   ```

2. Проверьте подключение к БД:
   ```bash
   psql -h localhost -U evgeniyyanvarskiy -d fox_crypto -c "SELECT COUNT(*) FROM monitoring.positions;"
   ```

3. Запустите тест приложения:
   ```bash
   cd monitor_ui
   source venv/bin/activate
   python test_app.py
   ```

## Troubleshooting

### Проблема: "Cannot connect to database"

**Решение:**
```bash
# Проверьте что PostgreSQL работает
pg_isready

# Проверьте параметры подключения
python main.py --db-host localhost --db-port 5432 --db-name fox_crypto
```

### Проблема: "No module named 'textual'"

**Решение:**
```bash
# Убедитесь что виртуальное окружение активировано
source venv/bin/activate

# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема: "No data displayed"

**Решение:**
```bash
# Проверьте наличие данных в БД
psql -U evgeniyyanvarskiy -d fox_crypto -c "
  SELECT COUNT(*) FROM monitoring.positions WHERE status='active';
  SELECT COUNT(*) FROM monitoring.events WHERE created_at > NOW() - INTERVAL '1 hour';
"
```

### Проблема: "High CPU usage"

**Решение:**
```env
# Увеличьте интервалы обновления в .env
POSITION_UPDATE_INTERVAL=2.0
EVENT_UPDATE_INTERVAL=2.0
STATS_UPDATE_INTERVAL=15.0
```

## Логирование

### Просмотр логов

```bash
# В реальном времени
tail -f monitor_ui.log

# Последние 50 строк
tail -n 50 monitor_ui.log

# С фильтрацией по ошибкам
grep ERROR monitor_ui.log
```

### Уровни логирования

- `DEBUG` - Максимум информации (для отладки)
- `INFO` - Обычные операции
- `WARNING` - Предупреждения
- `ERROR` - Ошибки
- `CRITICAL` - Критические ошибки

## Производительность

**Нормальные показатели:**
- Запуск: < 2 секунды
- Обновление позиций: < 100ms
- Использование памяти: < 100MB
- Загрузка CPU: < 5% (в покое < 1%)

## Документация

- **README.md** - Полная документация
- **QUICKSTART.md** - Руководство быстрого старта
- **TEST_RESULTS.md** - Результаты тестирования
- **SYSTEM_SUMMARY.md** - Общий обзор системы
- **MONITORING_UI_IMPLEMENTATION_PLAN.md** - Детальный план реализации

## Поддержка

При возникновении проблем:

1. Проверьте TEST_RESULTS.md
2. Запустите с `--log-level DEBUG`
3. Проверьте логи в monitor_ui.log
4. Убедитесь что БД доступна

## Обновление

```bash
cd monitor_ui
source venv/bin/activate

# Обновить зависимости
pip install --upgrade -r requirements.txt

# Перезапустить приложение
python main.py
```

---

**Готово к использованию!** 🚀

Все тесты пройдены, система работает корректно.
