# Quick Start Guide

Быстрое руководство по запуску системы мониторинга Fox Crypto Trading Bot.

## Шаг 1: Подготовка окружения

```bash
cd /Users/evgeniyyanvarskiy/PycharmProjects/TBot_monitoring/monitor_ui

# Создать виртуальное окружение
python3 -m venv venv

# Активировать окружение
source venv/bin/activate  # macOS/Linux
```

## Шаг 2: Установка зависимостей

```bash
pip install -r requirements.txt
```

Будут установлены:
- `textual==0.47.1` - TUI фреймворк
- `asyncpg==0.29.0` - асинхронный PostgreSQL драйвер
- `pydantic==2.5.2` - валидация данных
- `rich==13.7.0` - форматирование текста
- `python-dotenv==1.0.0` - переменные окружения

## Шаг 3: Проверка подключения к БД

Убедитесь, что PostgreSQL запущен и доступен:

```bash
# Проверить что PostgreSQL работает
pg_isready

# Проверить подключение к БД
psql -h localhost -U evgeniyyanvarskiy -d fox_crypto -c "SELECT COUNT(*) FROM monitoring.positions;"
```

## Шаг 4: Запуск монитора

### Вариант 1: Запуск с настройками по умолчанию

```bash
python main.py
```

### Вариант 2: С кастомными параметрами

```bash
# С отладочными логами
python main.py --log-level DEBUG

# С удаленной БД
python main.py --db-host remote.host --db-user myuser

# С логами в файл
python main.py --log-file monitor.log
```

## Шаг 5: Использование интерфейса

После запуска вы увидите терминальный интерфейс с несколькими панелями:

### Клавиши управления:

- `Q` - Выход
- `R` - Обновить данные принудительно
- `C` - Очистить поток событий
- `P` - Пауза/возобновление обновлений
- `↑/↓` - Навигация по таблице позиций

### Панели интерфейса:

1. **Header** (верх) - статус системы, аптайм, количество позиций
2. **Positions Table** (слева) - активные позиции в реальном времени
3. **Statistics** (справа вверху) - статистика за последний час
4. **Alerts** (справа внизу) - предупреждения и алерты
5. **Event Stream** (низ) - поток событий системы

## Частые проблемы

### Ошибка подключения к БД

```
Error: connection to database failed
```

**Решение**: Проверьте параметры подключения:
```bash
python main.py --db-host localhost --db-port 5432 --db-name fox_crypto --db-user evgeniyyanvarskiy
```

### Нет данных в интерфейсе

**Возможные причины**:
1. В БД нет активных позиций
2. Неправильная схема БД (должна быть `monitoring`)
3. Нет прав на чтение таблиц

**Проверка**:
```sql
-- Проверить наличие данных
SELECT COUNT(*) FROM monitoring.positions WHERE status = 'active';
SELECT COUNT(*) FROM monitoring.events WHERE created_at > NOW() - INTERVAL '1 hour';
```

### Высокая нагрузка CPU

**Решение**: Увеличить интервалы обновления в `.env`:
```env
POSITION_UPDATE_INTERVAL=2.0
EVENT_UPDATE_INTERVAL=2.0
STATS_UPDATE_INTERVAL=15.0
```

## Конфигурация через .env

Создайте файл `.env` в корне проекта:

```env
# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fox_crypto
DB_USER=evgeniyyanvarskiy
DB_PASSWORD=

# Update intervals (seconds)
POSITION_UPDATE_INTERVAL=1.0
EVENT_UPDATE_INTERVAL=1.0
STATUS_UPDATE_INTERVAL=5.0
STATS_UPDATE_INTERVAL=10.0

# UI Settings
MAX_EVENTS_DISPLAY=50
POSITION_AGE_WARNING_HOURS=12
POSITION_AGE_CRITICAL_HOURS=24

# Logging
LOG_LEVEL=INFO
LOG_FILE=monitor_ui.log
```

## Тестирование

Запуск в режиме отладки:

```bash
# Максимальные логи
python main.py --log-level DEBUG --log-file debug.log

# Проверка подключения к БД
python -c "import asyncio; from database.connection import DatabasePool; asyncio.run(DatabasePool.test_connection())"
```

## Что дальше?

После успешного запуска:

1. ✅ Проверьте что позиции отображаются
2. ✅ Убедитесь что события появляются в реальном времени
3. ✅ Проверьте статистику
4. ✅ Настройте алерты под свои нужды

## Помощь

Полная документация: [README.md](README.md)
Детальный план: [MONITORING_UI_IMPLEMENTATION_PLAN.md](../MONITORING_UI_IMPLEMENTATION_PLAN.md)

---

**Приятного мониторинга! 📊**
